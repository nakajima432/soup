%header.page-header.wrapper
  .title-list
    = link_to '/', class: "home-link" do
      = icon('fas', 'mug-hot',class: 'soup-icon')
      %p.title
        Soup
    %h6.sub-title
      \- 子ども食堂交流コミュニティ -
  %nav
    %ul.main-nav
      %li
        %a{href: new_cafeteria_path} 投稿する
      %li
        %a{href: cafeterias_path} 食堂一覧
      - if user_signed_in?
        %li
          = link_to user_path(current_user), class: "my-page" do
            %figure.image.is-48x48
              = attachment_image_tag current_user, :profile_image, fallback: "images/no-image.png"
            %p.current-username
              = current_user.username
        %li
          = link_to "ログアウト", destroy_user_session_path, method: :delete
      - else
        %li
          = link_to "ログイン", new_user_session_path

