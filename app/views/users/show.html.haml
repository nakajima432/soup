#header.big-bg
  .wrapper
    = render "shared/header"
  .page-title #{@user.username}さんの投稿一覧
%section.section
  .container
    .columns.is-centered
      .column.is-8
        .columns.is-centered
          .column.is-4
            %figure.image.is-128x128{style: "margin-left: auto; margin-right: auto; border-radius: 50%;"}
              = link_to edit_user_path do
                = attachment_image_tag @user, :profile_image, fallback: "images/no-image.png"
          .column.is-8
            %table.table.is-fullwidth
              %tr
                %td.is-size-4
                  %strong= @user.username
                - if @user.id == current_user.id
                  %td.is-size-4
                    = @user.email
                  %td
                    = link_to "編集", edit_user_path(@user), class: "button is-primary"
            %table.table.is-fullwidth
              %tr
                %td
                  = @user.profile

%section.section
  .cafeterias-container
    .columns.is-multiline
      - @cafeterias.each do |cafeteria|
        = render partial: "cafeterias/cafeteria", locals: { cafeteria: cafeteria }
= render "shared/footer"

